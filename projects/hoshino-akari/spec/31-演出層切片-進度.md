# 演出層切片 —— 進度留底（Step 1 / 2 / 3 / 2.5）

> 起點：`spec/20-診斷與強化建議.md` 判定「文字 A 級，但缺可實裝演出層、選項半數空轉」。
> 對照實際程式碼後校正：SNS 推薦欄 UI、Day4 真選項、熱可可/布丁純演出皆已實裝；
> 真正缺口收斂為 (A) 未讀紅點視覺化、(B) camera 原語。以下為切片化執行進度。

## Step 1 —— 未讀紅點 + camera 原語（已實裝）

**狀態：contract-pass / route-pass / visual-pending**

- A 未讀紅點：把 Day4 已 set 的 `unread_message_flag` 顯影成右上角壓力計（純 UI 計數，不碰 story flag）。
  指令 `{ ui:"unread_badge", unread:{ op:"inc"|"hold"|"clear", key, by } }`；每場 scene 自行計數、自清。
- B camera 原語：`push / pan / hold / reset`，只作用視覺三層（moodbg/spriteLayer/cgLayer），
  不動對白框・選項・SNS・紅點；與 sprite 的 pos/depth/motion 互補不衝突。
  push 上限 1.08、pan 同步微縮放 1.05 補邊；skip / reduced-motion 時 duration 收斂為 0。
- Day4 接線：停車場手機三次震動 inc、反扣 hold、離場 clear；公司/回家 SNS 慢推；換場/換日自動 reset。
- validator：新增 `line.ui` / `line.camera.op` / `line.unread.op` 白名單（additive）。
- 動到的層：engine.js（新增模組）、style.css、index.html、play/data/day4.js、tools/story_schema.js。

## Step 2 —— Day6 回收 + flavor 標記（已實裝）

**狀態：contract-pass / route-pass / visual-pending**

- 2-A Day6 紅點重用：D6-S4 經理人壓力段以 gate `cond:"flag:unread_message_flag"` 讀取 Day4 旗標
  （`unread_message_flag` 從孤兒 flag 變成有正式 reader），紅點回聲 inc/inc/hold ＋ camera push。
  此 gate 定位為 **compatibility / fallback gate**，非劇情分歧：正常新遊戲必進 then；
  else 僅供舊 save／debug／未來 Day4 改為可跳過時的安全回退。換場到 D6-S5 自動清紅點。
- 2-B flavor 標記：全劇掃描後只有 `d3door / d5s4 / d7s7` 三個選擇無任何 add/flag＝真風味，
  標 `choiceType:"flavor"`。validator 新增：未知 choiceType → ERROR；flavor 選項帶 add/flag → ERROR。
- engine.js 未改（Step 1 原語已足）。動到 day3/5/6/7.js、tools/story_schema.js。

## 驗證現況（Step 1 + 2）

- `node tools/validate_story.js` → 0 error / 0 warning。
- `node tools/qa_smoke.js` → 四結局全可達（gate 9→10、choice 仍 20）。
- `node --check` 全過；validator 新規則皆有負向測試確認觸發（非死規則）。
- 契約守住：未動 judge / ending / save 格式；camera 不影響 UI 層；unread clear 只清視覺不清 story flag。

## 待辦

1. **Now（gate）**：Day4 + Day6 人工實機 visual QA（見 `tools/QA-step1-2-visual.md`）。
   九項跨場 gate 全 PASS 才標 `visual-pass` 並凍結接線。
2. **Step 3 — flavor choice visual treatment**（visual-pass 後）：engine 讀 `choiceType` 加 class ＋ CSS 淡樣式。
   原則：淡但不像 disabled、不顯「flavor／語氣」字、不加 icon、hover 照常可點。
3. **Step 2.5 — Day2-S2／Day5 camera polish**（優先級最低）：少量補點，不橫向擴散。

> 風險：camera/紅點視覺體感未實機目視前，push 幅度、紅點 pulse、手機直幅裁切、reduced-motion 體感皆未知。
> 在未取得 visual-pass 前不再橫向新增 camera 接點。
